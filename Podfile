# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FirebaseAssessment' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FirebaseAssessment

  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Auth'
  pod 'Firebase/Storage'
  pod 'PKHUD', '~> 4.0'
  pod 'SDWebImage', '~>3.8'
  target 'FirebaseAssessmentTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FirebaseAssessmentUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
